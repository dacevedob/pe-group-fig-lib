% Compile with XeLaTeX or LuaLaTeX
% This preamble is used to set up the document for the PE Group in Inkscape.

% Install the plugin TexText from https://textext.github.io/textext/
% This plugin allows you to use LaTeX in Inkscape using any of its extensions, namely, pdfLaTeX,
% XeLaTeX or LuaLaTeX.
% The default Formula(pdflatex) is limited to LaTeX extension pdfLaTeX, then it cannaot use
% the system fonts.

% It sets the main and math font to Cambria

\documentclass[10pt]{article}

\usepackage{microtype} % Improves text appearance with better spacing
\usepackage{amsmath,amssymb,amsfonts}

\usepackage{fontspec}
\usepackage{unicode-math}
\setmainfont{Cambria}
%\setsansfont{Segoe UI}[Scale=MatchLowercase]
%\setmonofont{Lucida Console}[Scale=MatchLowercase]
\setmathfont{Cambria Math}[Scale=MatchLowercase]

%EOF
